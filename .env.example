# Prisma (SQLite)
DATABASE_URL="file:./dev.db"

# Used for absolute URLs in sitemap / RSS
NEXT_PUBLIC_SITE_URL="https://popcology.example"

# Optional: protect /admin and /api/admin with Basic Auth
# If omitted, the editor is accessible (useful for local dev).
ADMIN_USER="evan"
ADMIN_PASS="change-me"
